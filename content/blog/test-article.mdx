---
title: "Test Blog Post"
description: "Testing Fumadocs blog setup on vantageteam.dev"
date: "2026-04-03"
author: "VantageTeam"
tags: ["test"]
category: "repo-analysis"
source_type: "repo"
lang: "en"
---

## What is this?

This is a test article to verify that the Fumadocs blog setup is working correctly on vantageteam.dev. If you can read this, the MDX rendering pipeline is operational.

## Why it matters

A working blog is the foundation for publishing repo analyses, video breakdowns, and technical insights from the VantageTeam AI agent system.

## What you can use it for

- Verify that `/blog` renders the index page with this article listed
- Verify that `/blog/test-article` renders this MDX content
- Confirm dark mode, typography, and OKLCH tokens are applied
- Confirm the search API at `/api/search` is responding

## Key takeaways

### 1. Fumadocs powers the blog

Fumadocs handles MDX parsing, table of contents, search, and static param generation. It integrates cleanly with Next.js 15 App Router.

### 2. No i18n wrapping on /blog

The middleware excludes `/blog` from next-intl routing, so blog routes serve directly without locale prefixes. This is intentional — the blog handles language via the `lang` frontmatter field.

### 3. OKLCH tokens bridge the gap

The `--color-fd-*` variables are mapped to our OKLCH design tokens in `globals.css`, ensuring the blog inherits the site's visual identity without custom Fumadocs theme configuration.

## The bottom line

If this page renders correctly, the blog infrastructure is ready for production content.

---

*This article was generated by the VantageTeam agent system to verify the blog setup.*

[Explore our services →](/build)
